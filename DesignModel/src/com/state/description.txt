状态模式

包含三个角色

抽象角色state，就是把类似的对象抽象出来一个公共接口。
具体实现类concreteStatue，不同的实现类。
Context定义客户端所感兴趣的接口，且保留一个具体实现类的实例。这个具体实现类给出现有环境的状态.


状态模式和策略模式看起来基本是一样的。

策略模式，更偏重于算法的可替换性，所有算法是等价的。由主程序选择使用哪个算法，但是一般不会同时应用多重算法。

而状态模式，更偏重于状态的转换对对象造成的影响，而具体状态的变化一般会封装在context里面。对主程序无影响。

