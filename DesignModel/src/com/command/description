命令模式：

命令模式，主要是把命令进行抽象，从而对客户端和真正执行者之间实现解耦。

命令模式有5个角色
Command:定义命令的接口，声明执行的方法。
ConcreteCommand:命令接口实现对象，里面会有真正的接收者，并调用接收者的功能来完成命令要执行的操作。
Receiver:接收者，真正执行命令的对象。任何类都可能成为一个接收者，只要它能够实现命令要求实现的相应功能。
Invoker:负责调用命令对象执行请求。
Client:创建具体的命令对象并指明接收者。

举个例子：
比如你屋子里现在有两台设备，电脑和手机。
你点击开机键的时候，其实他们本身做了很多不同的操作，比如给硬件通电，然后加载操作系统等等。
这时候，屋子这个环境就是client，它有一台电脑，一台手机，都是receiver。
而你是invoker，你来去点击电脑上的开机，或者是点击手机上的开机。
而开机这个动作就是command，不同的开机结果，就是不不同的ConcreteCommand。






